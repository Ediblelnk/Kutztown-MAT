\input{../../templates/packages-article.tex}
\input{../../templates/macros.tex}

\lhead{Discrete Math II}
\chead{Section 7.1}
\rhead{Peter Schaefer}

\begin{document}

\section*{Section 7.2}

\subsection*{7.2.1}
Characterize the rate of growth of each function $f$ below by giving a function $g$ such that $f = \Theta(g)$. The function $g$ should be one of the functions in the table of common functions.
\begin{enumerate}
  \biitem{a}{$f(n) = n^8 + 3n -4$} $\qquad \rightarrow \qquad \Theta(n^8)$
  \biitem{b}{$f(n) = 2 \cdot 3^n$} $\qquad \rightarrow \qquad \Theta(3^n)$
  \biitem{c}{$f(n) = 2^n + 3^n$} $\qquad \rightarrow \qquad \Theta(3^n)$
  \biitem{d}{$f(n) = 7(\log\log n) + 3(\log n) + 12n$} $\qquad \rightarrow \qquad \Theta(n)$
  \biitem{e}{$f(n) = 9(n\log n) + 5(\log\log n) + 5$} $\qquad \rightarrow \qquad \Theta(n\log n)$
  \biitem{f}{$f(n) = n \cdot \log_{37} n$} $\qquad \rightarrow \qquad \Theta(n\log n)$
  \biitem{g}{$f(n) = n^{21} + (1.1)^n$} $\qquad \rightarrow \qquad \Theta(1.1^n)$
  \biitem{h}{$f(n) = 23n + n^3 - 2$} $\qquad \rightarrow \qquad \Theta(n^3)$
\end{enumerate}

\subsection*{7.2.2}
Give complete proofs for the growth rates of the polynomials below. You should provide specific values for $c \tand n_0$, and prove algebraically that the functions satisfy the definitions for $\mathcal{O} \tand \Omega$.
\begin{enumerate}
  \biitem{b}{$f(n) = n^3 + 3n^2 + 4$. Prove that $f = \Theta(n^3)$}
  \begin{proof}
    \itl{of $\mathcal{O}(n^3)$}:
    Consider $n_0 = 1 \tand c = 8$:
    For $n \geq 1,~1 \leq n^2$, so:
    \[
      n^3 + 3n^2 + 4 \leq n^3 + 3n^2 + 4n^2.
    \]
    For $n \geq 1,~n^2 \leq n^3$, so:
    \[
      n^3 + 3n^2 + 4n^2 \leq n^3 + 3n^3 + 4n^3.
    \]
    \begin{align*}
      f(n) = n^3 + 3n^2 + 4 \leq n^3 + 3n^2 + 4n^2 \leq n^3 + 3n^3 + 4n^3 = 8n^3 = 8(n^3).
    \end{align*}
    Therefore, with witness $n_0 = 1 \tand c = 8$, $f = \mathcal{O}(n^3)$.

    \itl{Proof. of $\Omega(n^3)$}:
    Consider $n_0 = 1 \tand c = 1$:
    For $n \geq 1,~3n^2 \geq 0 \tand 4 \geq 0$. Adding these inequalities yields
    \[
      3n^2 + 4 \geq 0
    \]
    Adding $n^3$ to both sides to get
    \[
      n^3 + 3n^2 + 4 \geq n^3
    \]
    Therefore, with witness $n_0 = 1 \tand c = 1$, $f = \Omega(n^3)$.

    Since $f = \mathcal{O}(n^3) \tand f = \Omega(n^3),~\therefore f = \Theta(n^3)$.
  \end{proof}
\end{enumerate}

\end{document}