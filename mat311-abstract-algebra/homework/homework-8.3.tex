\documentclass{article}
\input{../../.latex/article.tex}
\input{../../.latex/macros.tex}

\newcommand{\assignment}{Section 8, p83 \#30-33, 35, 40-42}

\lhead{MAT 311 Abstract Algebra}
\chead{\assignment}
\rhead{Peter Schaefer}

\begin{document}
\section*{\assignment}

In Exercises 30 through 34, determine whether the given function is a permutation of $\bb{R}$.
\begin{enumerate}
    \qitem{30}{$f_1: \bb{R} \rightarrow \bb{R}$ defined by $f_1(x) = x+1$}{answer}
    \qitem{31}{$f_2: \bb{R} \rightarrow \bb{R}$ defined by $f_2(x) = x^2$}{answer}
    \qitem{32}{$f_3: \bb{R} \rightarrow \bb{R}$ defined by $f_3(x) = -x^3$}{answer}
    \qitem{33}{$f_4: \bb{R} \rightarrow \bb{R}$ defined by $f_4(x) = e^x$}{answer}
    \qitem{35}{Mark each of the following:}{
        true or false
        \begin{enumerate}
            \qitem{a}{Every permutation is a ono-to-one function}{answer}
            \qitem{b}{Every function is a permutation if and only if it is one-to-one}{answer}
            \qitem{c}{Every function from a finite set onto itself must be one to one}{answer}
            \qitem{d}{Every group $G$ is isormorphic to a subgroup of $S_G$}{answer}
        \end{enumerate}
    }
\end{enumerate}
\bld{Theory}

In Exercises 40 through 43, let $A$ be a set, $B$ a subset of $A$, and let $b$ be one particular element of $B$. Determine whether the given set is sure to be a subgroup of $S_A$ under the induced operation. Here $\sigma[B] = \{\sigma(x) : x \in B\}$.
\begin{enumerate}
    \qitem{40}{$\{\sigma \in S_A : \sigma(b) = b\}$}{answer}
    \qitem{41}{$\{\sigma \in S_A : \sigma(b) \in B\}$}{answer}
    \qitem{42}{$\{\sigma \in S_A : \}$}{}
\end{enumerate}

\end{document}